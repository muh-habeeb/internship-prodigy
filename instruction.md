⚙️ In your case:

Since you’re building a Node.js + Mongoose backend (typical Express app):

✅ Use bcrypt if it installs cleanly.
🩹 If you face “node-gyp rebuild” or “python not found” errors — just switch to bcryptjs; your logic remains the same.